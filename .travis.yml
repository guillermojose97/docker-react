language: generic 

sudo: required
services:
  - docker

before_install:
  - docker build -t guillermojose/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true guillermojose/docker-react npm run test
  